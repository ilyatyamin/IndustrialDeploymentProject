apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "muffin-wallet.fullname" . }}-test-connection"
  labels:
    {{- include "muffin-wallet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "muffin-wallet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
